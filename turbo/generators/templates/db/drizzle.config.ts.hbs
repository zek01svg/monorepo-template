import type { Config } from "drizzle-kit";

if (!process.env.{{ slug }}_POSTGRES_URL) {
  throw new Error("Missing {{ slug }}_POSTGRES_URL");
}

const nonPoolingUrl = process.env.{{ slug }}_POSTGRES_URL.replace(":6543", ":5432");

export default {
  schema: "./src/schema.ts",
  dialect: "postgresql",
  dbCredentials: {
    url: nonPoolingUrl + "?sslmode=require",
  },
  casing: "snake_case",
} satisfies Config;
