{
  "name": "@repo/vitest",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./base": {
      "types": "./dist/configs/base-config.d.ts",
      "default": "./dist/configs/base-config.js"
    },
    "./ui": {
      "types": "./dist/configs/ui-config.d.ts",
      "default": "./dist/configs/ui-config.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "collect-json-reports": "node dist/scripts/collect-json-outputs.js",
    "merge-json-reports": "nyc merge coverage/raw coverage/merged/merged-coverage.json",
    "report": "nyc report -t coverage/merged --report-dir coverage/report --reporter=html --exclude-after-remap false",
    "view-report": "pnpm merge-json-reports && pnpm report && open coverage/report/index.html",
    "clean": "git clean -xdf .cache .turbo node_modules coverage.json coverage"
  },
  "dependencies": {
    "vite-tsconfig-paths": "^5.1.4",
    "vitest": "catalog:vitest"
  },
  "devDependencies": {
    "@repo/tsconfig": "workspace:*",
    "@types/minimatch": "^5.1.2",
    "@vitest/coverage-istanbul": "catalog:vitest",
    "@vitest/ui": "catalog:vitest",
    "glob": "catalog:",
    "jsdom": "^26.0.0",
    "nyc": "^17.1.0",
    "typescript": "latest"
  }
}
