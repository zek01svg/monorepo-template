{
  "name": "@repo/temporal-common",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./env": "./src/env.ts"
  },
  "scripts": {
    "build": "tsc",
    "clean": "git clean -xdf .cache .turbo dist node_modules coverage.json",
    "dev": "tsc",
    "format": "oxfmt --check",
    "format:fix": "oxfmt --write .",
    "lint": "oxlint --type-aware -c ../../tooling/eslint/oxlint.json",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false",
    "test": "vitest run"
  },
  "dependencies": {
    "@repo/service-discovery": "workspace:^",
    "@temporalio/client": "^1.12.1",
    "@temporalio/common": "^1.12.1",
    "vitest": "catalog:vitest"
  },
  "devDependencies": {
    "@repo/tsconfig": "workspace:*",
    "@repo/vitest": "workspace:*",
    "typescript": "catalog:"
  },
  "prettier": "@repo/prettier-config"
}
