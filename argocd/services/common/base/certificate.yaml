apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: postgres-server-tls
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
spec:
  issuerRef:
    kind: ClusterIssuer
    name: selfsigned-cluster-issuer
  secretName: postgres-server-tls
  secretTemplate:
    labels:
      cnpg.io/reload: "true"
  usages:
    - server auth
  dnsNames:
    - postgres.127.0.0.1.nip.io
