apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: auth
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  cluster:
    name: cnpg-cluster
  name: auth
  owner: admin
  ensure: present
